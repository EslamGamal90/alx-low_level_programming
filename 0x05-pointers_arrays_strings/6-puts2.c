#include "main.h"
#include <stdio.h>
/**
 * puts2 - This fuction print a string reverser
 *
 * @str: this is the pointer that point to a string
 */

void puts2(char *str)
{
	int x;

	for (x = 0; str[i] != '\0'; x++)
		{
		_putchar(str[i]);
		x++;
		}
		_putchar('\n');
}
