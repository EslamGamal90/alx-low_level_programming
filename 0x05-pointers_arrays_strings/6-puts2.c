#include "main.h"
#include <stdio.h>
/**
 * puts2 - This fuction print a string reverser
 *
 * @str: this is the pointer that point to a string
 */

void puts2(char *str)
{
	for (int x = 0; s[i] != '\0'; x++)
		{
		_putchar(s[i]);
		x++;
		}
		_putchar('\n');
}
