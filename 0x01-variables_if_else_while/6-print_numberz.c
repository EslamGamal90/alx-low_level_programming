#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
 * main - Print if number is positive, Zero or negative
 * Return: Always 0 (Success)
 *
 */
int main(void)
{
	int b;

	for (b = '0'; b <= '9'; b++)
	putchar(b);
	putchar('\n');
	return (0);
}
